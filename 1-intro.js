// Load the full build.
var _jasnaLodash = require('lodash');

const secret = `SUPER SECRET`
const john = `john`
const peter = `peter`

const funkcija = () => {
    _jasnaLodash.
}

module.exports = {john,peter}
